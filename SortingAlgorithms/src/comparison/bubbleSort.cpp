#include <vector>
#include <cstdlib>



void BubbleSort(std::vector<int>)
{
	int size =
		for (int i = 0; i < size; i++)
		{
			for (int j = i + 1; i < size; j++)
			{
				auto first = arr[i];
				auto second = arr[j];

				if (first > second)
				{

				}

			}
		}

}